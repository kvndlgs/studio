
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reads and writes for all users.
    // Warning: This is insecure. You should restrict access to your database.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
